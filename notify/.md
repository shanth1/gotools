# Examples

```go
func main() {
	notifyService, err := notify.New(notify.WithTelegram(token, chatID))
	if err != nil {
		logger.Fatal().Err(err).Msg("new notify service")
	}

	if err := notifyService.Send(context.Background(), "hi"); err != nil {
		fmt.Println("send message error:", err)
	}
}
```
